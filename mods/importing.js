import * as t from 'babel-types';

// just to see what I've got from babel-types
// Object.keys(t)
//   .filter(k => k.match(/^(is)([a-z])+/igm))
//   .map(k => console.log(k));

module.exports = function (path) {

};